# 计划：路径二 v1 Collection 接入写作流程
**日期：2026-04-28**
执行环境：参见 `docs/opencode_dev_protocol_20260420.md v1`
分支：**master**（唯一分支）
日志目录：`docs/m7_artifacts/`

---

## 背景

路径二（unified_extractor）已向 Qdrant 写入 8 个 v1 collection：
`dialogue_style_v1 / emotion_arc_v1 / power_vocabulary_v1 / foreshadow_pair_v1 /
power_cost_v1 / character_relation_v1 / worldview_element_v1 / author_style_v1`

但当前 5 个写手 skill 只检索 `writing_techniques_v2` 和 `case_library_v2`，
8 个 v1 collection 完全闲置。

本计划分两层：
- **V 系列（Python）**：给 `hybrid_search_manager.py` 补两个缺失方法，更新场景路由表
- **K 系列（SKILL）**：5 个写手 skill 各加对应 v1 collection 检索段落

⚠️ SKILL.md 文件在 `C:\Users\39477\.agents\skills\novelist-*\SKILL.md`，
**不在 git 仓库内**，V 系列 Python 修改正常 commit，K 系列 SKILL 修改直接写入文件但不 commit。

---

## V1 — `search_power_cost()` 方法（hybrid_search_manager.py）

**文件**：`modules/knowledge_base/hybrid_search_manager.py`

在 `search_character_relation()` 方法之后（约第 878 行，`retrieve_for_scene` 之前）插入：

```python
    def search_power_cost(
        self,
        query: str,
        power_type: Optional[str] = None,
        top_k: int = 10,
    ) -> List[Dict[str, Any]]:
        """检索力量代价描写"""
        collection_name = COLLECTION_NAMES.get("power_cost")
        if not collection_name:
            return []

        client = self._get_client()
        collections = [c.name for c in client.get_collections().collections]
        if collection_name not in collections:
            return []

        query_vectors = self._encode_query(query)

        try:
            results = client.query_points(
                collection_name=collection_name,
                query=query_vectors["dense"],
                using="dense",
                limit=top_k,
                with_payload=True,
            )

            formatted = []
            for p in results.points:
                formatted.append(
                    {
                        "id": p.id,
                        "text": p.payload.get("text", ""),
                        "power_type": p.payload.get("power_type", ""),
                        "cost_category": p.payload.get("cost_category", ""),
                        "scene_context": p.payload.get("scene_context", ""),
                        "score": p.score,
                    }
                )

            if power_type:
                formatted = [
                    r for r in formatted if power_type in r.get("power_type", "")
                ]

            return formatted
        except Exception as e:
            print(f"力量代价检索错误: {e}")
            return []

    def search_author_style(
        self,
        query: str,
        genre: Optional[str] = None,
        top_k: int = 10,
    ) -> List[Dict[str, Any]]:
        """检索作者风格段落"""
        collection_name = COLLECTION_NAMES.get("author_style")
        if not collection_name:
            return []

        client = self._get_client()
        collections = [c.name for c in client.get_collections().collections]
        if collection_name not in collections:
            return []

        query_vectors = self._encode_query(query)

        try:
            results = client.query_points(
                collection_name=collection_name,
                query=query_vectors["dense"],
                using="dense",
                limit=top_k,
                with_payload=True,
            )

            formatted = []
            for p in results.points:
                formatted.append(
                    {
                        "id": p.id,
                        "text": p.payload.get("text", ""),
                        "genre": p.payload.get("genre", ""),
                        "style_label": p.payload.get("style_label", ""),
                        "score": p.score,
                    }
                )

            if genre:
                formatted = [r for r in formatted if genre in r.get("genre", "")]

            return formatted
        except Exception as e:
            print(f"作者风格检索错误: {e}")
            return []
```

---

## V2 — 更新 `retrieve_for_scene()` source_map

**文件**：`modules/knowledge_base/hybrid_search_manager.py`

找到 `retrieve_for_scene()` 方法内的 `source_map` 字典（约第 902 行），
将以下 key 的 value 列表按下表修改：

| key | 原列表 | 修改后列表 |
|-----|--------|-----------|
| `"战斗"` | `["technique", "case", "power_vocabulary"]` | `["technique", "case", "power_vocabulary", "power_cost"]` |
| `"高潮"` | `["technique", "case", "power_vocabulary", "emotion_arc"]` | `["technique", "case", "power_vocabulary", "emotion_arc", "power_cost"]` |
| `"修炼突破"` | `["technique", "case", "power_vocabulary"]` | `["technique", "case", "power_vocabulary", "power_cost"]` |
| `"环境"` | `["technique", "case", "worldview_element"]` | `["technique", "case", "worldview_element", "author_style"]` |
| `"结尾"` | `["technique", "case", "worldview_element"]` | `["technique", "case", "worldview_element", "author_style"]` |
| `"恢复休养"` | `["technique", "case"]` | `["technique", "case", "author_style"]` |

同时在 `for source in sources:` 循环（约第 938 行）里的最后一个 `elif` 分支之后追加：

```python
            elif source == "power_cost":
                results["power_cost"] = self.search_power_cost(query, top_k=top_k)
            elif source == "author_style":
                results["author_style"] = self.search_author_style(query, top_k=top_k)
```

---

## K1 — 剑尘 SKILL.md 追加 v1 检索段落

**文件**：`C:\Users\39477\.agents\skills\novelist-jianchen\SKILL.md`

在技法库层（`### 技法库层`）和案例库层之间，在文件中找到注释
`### 案例库层` 之前，**新增以下完整段落**：

```markdown
### 代价词汇层（v1 精细化参考）

**Collection**：`power_cost_v1`（战斗代价描写）+ `power_vocabulary_v1`（力量词汇）

```python
from modules.knowledge_base.hybrid_search_manager import HybridSearchManager
search = HybridSearchManager()

# 力量代价参考（从 5897 本小说提炼的真实战斗代价写法）
power_cost_refs = search.search_power_cost(
    query=f"[力量体系名]战斗代价 {scene_brief}",
    top_k=3
)
# 返回字段: text(代价描写段落), power_type(力量体系), cost_category(代价类型), scene_context

# 力量词汇参考
power_vocab = search.search_power_vocabulary(
    query=f"[力量体系名] 招式词汇",
    top_k=5
)
# 返回字段: text(词汇上下文), power_type, category
```

**如何使用结果**：
- `power_cost_refs` → 提取 `text` 字段作为"代价写法参考"注入 Phase 2 prompt 头部
- 格式：`【代价写法参考】\n{ref1.text}\n---\n{ref2.text}`
- `power_vocab` → 提取同体系的招式词汇，融入战斗动作描写
- **无结果时**：跳过，不影响主流程
```

---

## K2 — 墨言 SKILL.md 追加 v1 检索段落

**文件**：`C:\Users\39477\.agents\skills\novelist-moyan\SKILL.md`

找到 `### 案例库层` 之前，**新增以下完整段落**：

```markdown
### 对话风格层（v1 精细化参考）

**Collection**：`dialogue_style_v1`（对话风格）+ `emotion_arc_v1`（情感弧线）

```python
from modules.knowledge_base.hybrid_search_manager import HybridSearchManager
search = HybridSearchManager()

# 对话风格参考（真实小说的对话写法，按题材分类）
dialogue_refs = search.search_extended(
    "dialogue_style",
    query=f"[题材/势力名] {scene_brief} 对话",
    top_k=3
)
# 返回字段: content(对话段落), genre, style_label

# 情感弧线参考（情感变化节奏）
emotion_refs = search.search_extended(
    "emotion_arc",
    query=f"{scene_brief} 情感变化",
    top_k=3
)
# 返回字段: content(情感弧段落), emotion_sequence
```

**如何使用结果**：
- `dialogue_refs` → 提取 `content` 作为"对话风格参考"，注入 Phase 2 prompt
- 格式：`【对话风格参考（同题材写法）】\n{ref1.content}`
- `emotion_refs` → 作为"情感节奏锚点"，提醒情感弧要有起伏而非平铺
- **无结果时**：跳过，不影响主流程
```

---

## K3 — 玄一 SKILL.md 追加 v1 检索段落

**文件**：`C:\Users\39477\.agents\skills\novelist-xuanyi\SKILL.md`

找到 `### 案例库层` 之前，**新增以下完整段落**：

```markdown
### 伏笔配对层（v1 精细化参考）

**Collection**：`foreshadow_pair_v1`（伏笔配对）

```python
from modules.knowledge_base.hybrid_search_manager import HybridSearchManager
search = HybridSearchManager()

# 伏笔配对参考（真实小说的伏笔埋设+回收写法）
foreshadow_refs = search.search_extended(
    "foreshadow_pair",
    query=f"{scene_brief} 伏笔",
    top_k=3
)
# 返回字段: content(伏笔段落), setup_context, payoff_context, distance(章节跨度)
```

**如何使用结果**：
- `foreshadow_refs` → 提取 `setup_context` + `payoff_context` 对，作为伏笔节奏参考
- 注入 Phase 2 prompt：`【伏笔参考（真实小说的埋设-回收节奏）】\n埋设：{r.setup_context}\n回收：{r.payoff_context}`
- 优先使用 `distance > 3`（跨章伏笔），短距离伏笔参考价值较低
- **无结果时**：跳过，不影响主流程
```

---

## K4 — 苍澜 SKILL.md 追加 v1 检索段落

**文件**：`C:\Users\39477\.agents\skills\novelist-canglan\SKILL.md`

找到 `### 案例库层` 之前（或世界观检索相关位置），**新增以下完整段落**：

```markdown
### 世界观元素层（v1 精细化参考）

**Collection**：`worldview_element_v1`（世界观元素）+ `character_relation_v1`（人物关系）

```python
from modules.knowledge_base.hybrid_search_manager import HybridSearchManager
search = HybridSearchManager()

# 世界观元素参考（从真实小说提炼的世界观构建写法）
worldview_refs = search.search_worldview(
    query=f"[势力名/力量体系] {scene_brief}",
    top_k=3
)
# 返回字段: text(世界观描写段落), element_type, total_frequency

# 人物关系参考（相似关系的经典写法）
relation_refs = search.search_character_relation(
    query=f"{scene_brief} 人物关系",
    top_k=3
)
# 返回字段: text(关系描写段落), character1, character2
```

**如何使用结果**：
- `worldview_refs` → 提取同类型世界观元素的"写法密度"参考
- 格式：`【世界观写法参考】\n{ref.text[:200]}`
- `relation_refs` → 提取相似关系的描写方式，用于势力互动场景
- **无结果时**：跳过，不影响主流程
```

---

## K5 — 云溪 SKILL.md 追加 v1 检索段落

**文件**：`C:\Users\39477\.agents\skills\novelist-yunxi\SKILL.md`

找到 `### 案例库层` 之前（或意境/润色相关位置），**新增以下完整段落**：

```markdown
### 作者风格层（v1 精细化参考）

**Collection**：`author_style_v1`（作者风格）+ `emotion_arc_v1`（情感弧线）

```python
from modules.knowledge_base.hybrid_search_manager import HybridSearchManager
search = HybridSearchManager()

# 作者风格参考（从同题材作者的优质段落中提炼写法）
style_refs = search.search_author_style(
    query=f"[题材] {scene_brief} 意境",
    top_k=3
)
# 返回字段: text(风格段落), genre, style_label

# 情感弧线参考（结尾/环境场景的情绪收束方式）
emotion_refs = search.search_extended(
    "emotion_arc",
    query=f"{scene_brief} 情绪收束",
    top_k=2
)
# 返回字段: content(情感段落), emotion_sequence
```

**如何使用结果**：
- `style_refs` → 提取同题材优质段落，作为"意境密度参考"用于润色 Phase 3
- 格式：`【风格参考（同题材写法）】\n{ref.text[:300]}`
- `emotion_refs` → 作为"情绪收束节奏"参考，避免结尾情绪突然截断
- **无结果时**：跳过，不影响主流程
```

---

## 测试方案（4 阶段）

```bash
# Stage 1：语法检查
python -c "
import ast
ast.parse(open('modules/knowledge_base/hybrid_search_manager.py', encoding='utf-8').read())
print('OK: hybrid_search_manager.py syntax')
" 2>&1 | tee docs/m7_artifacts/stage1_v1_syntax_$(date +%Y%m%d_%H%M%S).log

# Stage 2：单元测试
python -m pytest tests/ -q -p no:warnings 2>&1 | tee docs/m7_artifacts/stage2_v1_pytest_$(date +%Y%m%d_%H%M%S).log

# Stage 3：断言检查
python -c "
import ast, inspect, sys
sys.path.insert(0, '.')
from modules.knowledge_base.hybrid_search_manager import HybridSearchManager

# V1: 两个新方法存在
assert hasattr(HybridSearchManager, 'search_power_cost'), 'V1 search_power_cost missing'
assert hasattr(HybridSearchManager, 'search_author_style'), 'V1 search_author_style missing'
print('OK: V1 两个新方法已添加')

# V1: 方法签名正确
import inspect
sig_pc = inspect.signature(HybridSearchManager.search_power_cost)
assert 'query' in sig_pc.parameters, 'V1 search_power_cost 缺 query 参数'
assert 'power_type' in sig_pc.parameters, 'V1 search_power_cost 缺 power_type 参数'
sig_as = inspect.signature(HybridSearchManager.search_author_style)
assert 'query' in sig_as.parameters, 'V1 search_author_style 缺 query 参数'
assert 'genre' in sig_as.parameters, 'V1 search_author_style 缺 genre 参数'
print('OK: V1 方法签名正确')

# V2: retrieve_for_scene source_map 包含 power_cost 和 author_style
import inspect
src = inspect.getsource(HybridSearchManager.retrieve_for_scene)
assert 'power_cost' in src, 'V2 retrieve_for_scene 未包含 power_cost'
assert 'author_style' in src, 'V2 retrieve_for_scene 未包含 author_style'
print('OK: V2 source_map 已更新')

# V2: 循环分支处理 power_cost 和 author_style
assert 'search_power_cost' in src, 'V2 循环分支缺 search_power_cost 调用'
assert 'search_author_style' in src, 'V2 循环分支缺 search_author_style 调用'
print('OK: V2 循环分支已添加')

# K 系列：SKILL 文件包含 v1 检索段落
import os
skill_base = r'C:/Users/39477/.agents/skills'
checks = {
    'novelist-jianchen': ['search_power_cost', 'power_cost_v1'],
    'novelist-moyan':    ['search_extended', 'dialogue_style', 'emotion_arc'],
    'novelist-xuanyi':   ['search_extended', 'foreshadow_pair'],
    'novelist-canglan':  ['search_worldview', 'search_character_relation'],
    'novelist-yunxi':    ['search_author_style', 'author_style_v1'],
}
for skill_name, keywords in checks.items():
    skill_path = os.path.join(skill_base, skill_name, 'SKILL.md')
    content = open(skill_path, encoding='utf-8').read()
    for kw in keywords:
        assert kw in content, f'K系列 {skill_name} SKILL.md 缺少 {kw}'
    print(f'OK: K系列 {skill_name} v1检索段落已添加')

print()
print('=== ALL STAGE 3 ASSERTIONS PASSED ===')
" 2>&1 | tee docs/m7_artifacts/stage3_v1_assert_$(date +%Y%m%d_%H%M%S).log

# Stage 4：全量测试基线
python -m pytest tests/ -q -p no:warnings 2>&1 | tee docs/m7_artifacts/stage4_v1_full_$(date +%Y%m%d_%H%M%S).log
```

**通过标准**：
- Stage 1：语法 OK
- Stage 2：passed ≥ 598（与基线持平）
- Stage 3：所有 `OK:` 断言通过
- Stage 4：passed ≥ 598，failed = 0

---

## 提交说明（仅 Python 变更）

```
feat: v1 collection 接入写作流程

V1: hybrid_search_manager 新增 search_power_cost() + search_author_style()
    - 与 search_worldview/search_power_vocabulary/search_character_relation 同模式
    - power_type/genre 可选过滤参数
V2: retrieve_for_scene() source_map 补充 power_cost + author_style 路由
    - 战斗/高潮/修炼突破 → 追加 power_cost
    - 环境/结尾/恢复休养 → 追加 author_style
    - 循环分支追加对应处理

（K系列 SKILL.md 已直接写入 C:/Users/39477/.agents/skills/，不在本 commit 内）
```
