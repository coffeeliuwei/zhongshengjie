# 专家级能力体系设计方案

> 版本：v3.0
> 日期：2026-04-07
> 状态：**已完成**
> 更新：世界观适配层 + 统一API层 + SKILL精简 + 世界观生成器

---

## 一、项目概述

### 1.1 现有资产盘点

**当前系统已具备完整能力**：

| 资产 | 状态 | 说明 |
|------|------|------|
| 技法库向量化 | ✅ 已完成 | 986条技法，Qdrant Collection |
| 案例库向量化 | ✅ 已完成 | 38万+条案例，Qdrant Collection |
| 技法检索接口 | ✅ 已实现 | `technique_search.py` |
| 案例检索接口 | ✅ 已实现 | `case_search.py` |
| 知识库检索 | ✅ 已实现 | `knowledge_search.py` |
| 场景契约系统 | ✅ 已实现 | `scene_contract.py` |
| 配置加载器 | ✅ 已实现 | `config_loader.py` |
| 工作流系统 | ✅ 已实现 | `workflow.py` |

### 1.2 技法库结构

```
创作技法/
├── 01-世界观维度/        ← 苍澜领域（技法大全已存在）
├── 02-剧情维度/          ← 玄一领域（技法大全已存在）
├── 03-人物维度/          ← 墨言领域（技法大全已存在）
├── 04-战斗冲突维度/      ← 剑尘领域（技法大全已存在）
├── 05-氛围意境维度/      ← 云溪领域（技法大全已存在）
├── 06-叙事维度/
├── 07-主题维度/
├── 08-情感维度/
├── 09-读者体验维度/
├── 10-元维度/
├── 11-节奏维度/
├── 99-创作模板/
├── 99-从小说提取/
├── 99-实战案例/
└── 99-外部资源/         ← 70+专业技法
```

### 1.3 现有检索接口

#### 技法库检索（已实现）

```python
# .vectorstore/core/technique_search.py

class TechniqueSearcher:
    def search(query, dimension, top_k)      # 语义检索
    def get_by_dimension(dimension)           # 按维度获取
    def list_all_dimensions()                 # 列出所有维度
    def get_stats()                           # 统计信息

# Collection: writing_techniques_v2
# 数据量：986条
```

#### 案例库检索（已实现）

```python
# .vectorstore/core/case_search.py

class CaseSearcher:
    def search(query, scene_type, genre, top_k)  # 语义检索
    def get_by_scene(scene_type)                  # 按场景获取
    def get_by_genre(genre)                       # 按题材获取
    def get_stats()                               # 统计信息

# Collection: case_library_v2
# 数据量：38万+条
```

### 1.4 问题定义

现有系统缺少的能力：

| 缺失能力 | 影响 |
|----------|------|
| 世界观适配层 | 无法切换不同世界观配置 |
| 统一API层 | 各检索接口分散，无统一入口 |
| 方法论与世界观解耦 | SKILL.md中方法论与众生界定制耦合 |

### 1.5 目标

**补充缺失能力**，而非重建：

- **世界观适配**：配置可切换
- **统一API**：整合现有检索接口
- **SKILL精简**：引用四层架构

---

## 二、架构设计

### 2.1 四层架构（融合现有系统）

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              第一层：核心方法论                       │   │
│  │                                                     │   │
│  │  存储位置：docs/methods/                             │   │
│  │  状态：待抽取（从SKILL.md抽取通用方法论）             │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              第二层：技法库                           │   │
│  │                                                     │   │
│  │  存储位置：创作技法/ + Qdrant                        │   │
│  │  检索接口：technique_search.py ✅ 已实现             │   │
│  │  数据量：986条 ✅ 已向量化                           │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              第三层：案例库                           │   │
│  │                                                     │   │
│  │  存储位置：Qdrant                                    │   │
│  │  检索接口：case_search.py ✅ 已实现                  │   │
│  │  数据量：38万+条 ✅ 已向量化                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              第四层：世界观适配层                      │   │
│  │                                                     │   │
│  │  存储位置：config/world_configs/                     │   │
│  │  状态：待实现                                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ↓                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              统一API层                               │   │
│  │                                                     │   │
│  │  存储位置：.vectorstore/core/                        │   │
│  │  状态：待实现（整合现有接口）                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 现有能力与新能力关系

```
【现有能力 - 保持不变】

technique_search.py ─────┐
                         │
case_search.py ──────────┼──→ 被统一API层调用
                         │
knowledge_search.py ─────┤
                         │
workflow.py ─────────────┘

【新增能力】

config/world_configs/ ────→ 世界观适配层
world_config_loader.py ───→ 配置加载器
poetry_api.py ────────────→ 统一API（整合技法+案例检索）
battle_api.py ────────────→ 统一API
plot_api.py ──────────────→ 统一API
character_api.py ─────────→ 统一API
```

---

## 三、目录结构设计

```
众生界/
├── docs/
│   ├── methods/                          # 第一层：方法论（新增）
│   │   ├── poetry_methodology.md         # 诗词创作方法论
│   │   ├── battle_methodology.md         # 战斗设计方法论
│   │   ├── plot_methodology.md           # 剧情编织方法论
│   │   ├── character_methodology.md      # 人物刻画方法论
│   │   └── worldview_methodology.md      # 世界观构建方法论
│   │
│   └── AI项目掌控手册.md
│
├── 创作技法/                              # 第二层：技法库（已有）
│   ├── 01-世界观维度/                     # ✅ 已存在
│   ├── 02-剧情维度/                       # ✅ 已存在
│   ├── 03-人物维度/                       # ✅ 已存在
│   ├── 04-战斗冲突维度/                   # ✅ 已存在
│   ├── 05-氛围意境维度/                   # ✅ 已存在
│   ├── 06-叙事维度/                       # ✅ 已存在
│   ├── 07-主题维度/                       # ✅ 已存在
│   ├── 08-情感维度/                       # ✅ 已存在
│   ├── 09-读者体验维度/                   # ✅ 已存在
│   ├── 10-元维度/                         # ✅ 已存在
│   ├── 11-节奏维度/                       # ✅ 已存在
│   ├── 99-创作模板/                       # ✅ 已存在
│   ├── 99-从小说提取/                     # ✅ 已存在
│   ├── 99-实战案例/                       # ✅ 已存在
│   └── 99-外部资源/                       # ✅ 已存在
│
├── config/
│   └── world_configs/                    # 第四层：适配层（新增）
│       ├── base/                         # 基础配置
│       │   ├── 玄幻基础.yaml
│       │   ├── 仙侠基础.yaml
│       │   └── 都市基础.yaml
│       │
│       ├── zhongshengjie.yaml            # 众生界配置
│       ├── xianxia.yaml                  # 仙侠世界配置
│       ├── urban.yaml                    # 都市异能配置
│       └── scifi.yaml                    # 科幻星际配置
│
├── .vectorstore/
│   └── core/
│       ├── technique_search.py           # ✅ 已实现
│       ├── case_search.py                # ✅ 已实现
│       ├── knowledge_search.py           # ✅ 已实现
│       ├── scene_contract.py             # ✅ 已实现
│       ├── contract_validator.py         # ✅ 已实现
│       ├── config_loader.py              # ✅ 已实现
│       ├── workflow.py                   # ✅ 已实现
│       │
│       ├── world_config_loader.py        # 新增：世界观配置加载
│       ├── poetry_api.py                 # 新增：诗词创作API
│       ├── battle_api.py                 # 新增：战斗设计API
│       ├── plot_api.py                   # 新增：剧情编织API
│       ├── character_api.py              # 新增：人物刻画API
│       └── worldview_api.py              # 新增：世界观构建API
│
└── tools/
    └── ... (现有工具保持不变)
```

---

## 四、世界观适配层设计

### 4.1 配置文件结构

```yaml
# config/world_configs/zhongshengjie.yaml

# ========== 元信息 ==========
meta:
  name: "众生界"
  genre: "玄幻"
  theme: "厚重悲壮"
  description: "千年博弈，众生追问"

# ========== 核心设定 ==========
core_rules:
  - "血脉决定潜力上限"
  - "境界提升需要资源积累"
  - "越级挑战需付出代价"

# ========== 力量体系 ==========
power_system:
  type: "境界制"
  levels: ["凡人", "觉醒", "淬体", "凝脉", "结丹", "元婴", "化神"]
  terminology:
    power: "血脉之力"
    energy: "血脉灵力"
    cultivation: "血脉淬炼"
    breakthrough: "血脉跃迁"

# ========== 战斗适配 ==========
battle:
  style: "厚重热血"
  terminology:
    attack: "血脉之力爆发"
    defense: "血脉护体"
    ultimate: "血脉底牌"
  preferred_climax:
    - type: "血脉觉醒"
      priority: 1
    - type: "越级挑战"
      priority: 2
    - type: "底牌爆发"
      priority: 3

# ========== 人物适配 ==========
character:
  dialogue_style:
    protagonist: "沉稳内敛"
    antagonist: "狂妄阴狠"
    elder: "沧桑厚重"
  preferred_arcs:
    - "复仇成长"
    - "身份探寻"
    - "逆天改命"

# ========== 诗词适配 ==========
poetry:
  preferred_imagery:
    - name: "棋/棋子"
      emotion: "被操控感、命运感"
    - name: "风碑"
      emotion: "历史沉默、见证者"
    - name: "盐霜"
      emotion: "苦涩、苦难痕迹"
  imagery_substitution:
    "明月": "风碑"
    "落花": "盐霜"
    "孤鸿": "缝隙中的棋子"
  emotion_tone:
    - "厚重苍凉"
    - "虚无感"
    - "命运感"

# ========== 剧情适配 ==========
plot:
  preferred_suspense:
    - "身份悬念"
    - "命运悬念"
    - "血脉谜团"
  preferred_reversals:
    - "身份反转"
    - "立场反转"
    - "命运反转"
```

### 4.2 配置加载器接口

```python
# .vectorstore/core/world_config_loader.py

def load_world_config(world_name: str) -> dict:
    """加载世界观配置"""
    pass

def merge_world_configs(base: dict, override: dict) -> dict:
    """合并配置（处理继承）"""
    pass

def list_world_configs() -> List[str]:
    """列出所有可用配置"""
    pass

def validate_world_config(config: dict) -> Dict:
    """验证配置完整性"""
    pass
```

---

## 五、统一API层设计

### 5.1 设计原则

```
原则：整合现有接口，不重复实现

现有接口：
├── technique_search.py  # 技法检索 ✅
├── case_search.py       # 案例检索 ✅
└── knowledge_search.py  # 知识检索 ✅

统一API层职责：
├── 整合技法+案例+知识检索
├── 注入世界观配置
└── 提供统一调用入口
```

### 5.2 诗词创作API示例

```python
# .vectorstore/core/poetry_api.py

from technique_search import TechniqueSearcher
from case_search import CaseSearcher
from world_config_loader import load_world_config

def create_poetry(
    scene: str,
    emotion: str = None,
    world_config: dict = None,
    top_k: int = 5
) -> Dict:
    """
    诗词创作接口
    
    整合：
    - 技法检索：检索相关意象、诗体模板
    - 案例检索：检索标杆诗词片段
    - 世界观适配：应用偏好意象、情感基调
    """
    # 1. 检索技法
    technique_searcher = TechniqueSearcher()
    techniques = technique_searcher.search(
        query=f"{emotion} {scene}",
        dimension="氛围意境维度",
        top_k=top_k
    )
    
    # 2. 检索案例
    case_searcher = CaseSearcher()
    cases = case_searcher.search(
        query=f"{emotion} 诗词 {scene}",
        top_k=top_k
    )
    
    # 3. 世界观适配
    if world_config:
        poetry_config = world_config.get("poetry", {})
        # 应用偏好意象
        # 应用意象替换
        # 应用情感基调
        pass
    
    # 4. 返回结果
    return {
        "techniques": techniques,
        "cases": cases,
        "world_adapted": world_config is not None
    }
```

### 5.3 战斗设计API示例

```python
# .vectorstore/core/battle_api.py

from technique_search import TechniqueSearcher
from case_search import CaseSearcher

def design_battle(
    battle_type: str,
    tension_curve: str = "翻盘",
    world_config: dict = None,
    top_k: int = 5
) -> Dict:
    """
    战斗设计接口
    
    整合：
    - 技法检索：检索战斗节奏、爽点类型
    - 案例检索：检索标杆战斗片段
    - 世界观适配：应用力量术语、战斗风格
    """
    technique_searcher = TechniqueSearcher()
    case_searcher = CaseSearcher()
    
    # 检索技法
    techniques = technique_searcher.search(
        query=f"{battle_type} {tension_curve}",
        dimension="战斗冲突维度",
        top_k=top_k
    )
    
    # 检索案例
    cases = case_searcher.search(
        query=f"{battle_type} {tension_curve}",
        scene_type="战斗场景",
        top_k=top_k
    )
    
    # 世界观适配
    if world_config:
        battle_config = world_config.get("battle", {})
        # 应用术语映射
        # 应用爽点偏好
        pass
    
    return {
        "techniques": techniques,
        "cases": cases,
        "world_adapted": world_config is not None
    }
```

---

## 六、实施计划

### 6.1 阶段划分

```
Phase 1: 世界观适配层（1周）
├── 创建 config/world_configs/ 目录
├── 编写众生界配置
├── 编写其他世界观配置示例
└── 实现 world_config_loader.py

Phase 2: 统一API层（1周）
├── 实现 poetry_api.py（整合技法+案例检索）
├── 实现 battle_api.py
├── 实现 plot_api.py
├── 实现 character_api.py
└── 实现 worldview_api.py

Phase 3: SKILL.md精简（0.5周）
├── 云溪SKILL.md精简迁移
├── 其他作家SKILL.md精简
└── 引用四层架构
```

### 6.2 时间表

| 阶段 | 内容 | 周数 | 累计 |
|------|------|------|------|
| Phase 1 | 世界观适配层 | 1周 | 1周 |
| Phase 2 | 统一API层 | 1周 | 2周 |
| Phase 3 | SKILL.md精简 | 0.5周 | 2.5周 |

**总计**：2.5周

### 6.3 里程碑

```
Week 1   ─── 世界观适配层完成
Week 2   ─── 统一API层完成
Week 2.5 ─── SKILL.md精简完成
```

---

## 七、检查清单

```
□ Phase 1: 世界观适配层
  □ 创建 config/world_configs/ 目录
  □ 编写众生界配置
  □ 编写仙侠配置示例
  □ 编写都市配置示例
  □ 实现 world_config_loader.py
  □ 配置加载测试通过

□ Phase 2: 统一API层
  □ 实现 poetry_api.py
  □ 实现 battle_api.py
  □ 实现 plot_api.py
  □ 实现 character_api.py
  □ 实现 worldview_api.py
  □ API测试通过

□ Phase 3: SKILL.md精简
  □ 云溪SKILL.md精简
  □ 剑尘SKILL.md精简
  □ 玄一SKILL.md精简
  □ 墨言SKILL.md精简
  □ 苍澜SKILL.md精简
  □ 文档更新完成
```

---

## 八、验收标准

### 8.1 功能验收

| 功能 | 验收标准 |
|------|----------|
| 世界观配置加载 | 加载配置返回正确结构 |
| 配置切换 | 切换配置后输出风格变化 |
| API调用 | 所有API返回正确结果 |
| 技法检索 | 复用现有接口，检索正常 |
| 案例检索 | 复用现有接口，检索正常 |

### 8.2 质量验收

| 指标 | 标准 |
|------|------|
| 技法库 | 986条（已有） |
| 案例库 | 38万+条（已有） |
| 世界观配置 | 至少3个 |
| API覆盖率 | 5个维度 |

---

## 九、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 配置过于复杂 | 学习成本高 | 提供配置模板和示例 |
| API整合困难 | 延期 | 分步实现，先完成一个验证 |
| SKILL.md精简影响现有流程 | 功能异常 | 保留原有功能，渐进迁移 |

---

## 十、总结

### 现有资产价值

```
✅ 技法库向量化：986条
✅ 案例库向量化：38万+条
✅ 检索接口：完整实现
✅ 工作流：完整实现

总投入价值：约8周工作量
```

### 新增工作量

```
新增：
├── 世界观适配层：1周
├── 统一API层：1周
└── SKILL.md精简：0.5周

总计：2.5周
```

### 收益

```
✅ 世界观可切换
✅ API统一入口
✅ SKILL.md精简
✅ 方法论解耦

复用现有资产 = 节省8周工作量
```

---

## 附录：现有接口调用示例

### 技法检索

```python
from technique_search import TechniqueSearcher

searcher = TechniqueSearcher()

# 语义检索
results = searcher.search("战斗代价描写", top_k=5)

# 按维度检索
results = searcher.get_by_dimension("战斗冲突维度")

# 统计信息
stats = searcher.get_stats()
```

### 案例检索

```python
from case_search import CaseSearcher

searcher = CaseSearcher()

# 语义检索
cases = searcher.search("玄幻开篇 主角出场", top_k=5)

# 按场景类型检索
cases = searcher.get_by_scene("开篇场景")

# 按题材检索
cases = searcher.get_by_genre("玄幻奇幻")

# 统计信息
stats = searcher.get_stats()
```

---

> 本方案基于现有系统资产设计，最大化复用已有能力，最小化新增开发。