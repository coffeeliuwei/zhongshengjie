# 计划：记忆召回优化与断点续传

> 日期：2026-04-28 02:10（Asia/Shanghai）
> 开发协议：docs/opencode_dev_protocol_20260420.md v1
> 分支：master（禁止创建新分支）
> 来源：基于 opencode 初稿 docs/superpowers/specs/2026-04-28-opencode-memory-optimization-and-checkpoint-design.md，修正 3 处 bug 并重构断点设计

---

## 背景与目标

初稿提出两个方向均正确，但存在以下问题需在实施前修正：

| # | 问题 | 原因 |
|---|------|------|
| B1 | `recommended_tequences[:3]` 拼写错误 | 原稿 line 295 typo，运行即 `NameError` |
| B2 | `checkpoint.phase == 5.5` | `phase: int` 无法持有浮点，JSON 反序列化后变 `5`，恢复逻辑失效 |
| B3 | `execute_scene_with_checkpoint()` 试图用 Python 编排 SKILL 工作流 | 本系统是**对话驱动**（Claude↔用户），无 Python 主循环，这个函数永远不会被调用 |
| B4 | `writer_scene_mapping.json` 独立文件 | 历史记忆点无 `writer_agent` 字段，缺少回退逻辑；独立配置文件增加维护成本 |

修正后的实施清单：

| 编号 | 文件 | 操作 |
|------|------|------|
| M1 | `core/inspiration/memory_point_sync.py` | 新增 2 个方法，含历史记录回退 |
| M2 | `config.json` | 新增 `writer_mapping` 节（替代独立 JSON 文件） |
| M3 | `core/inspiration/writer_memory_retriever.py` | 新建文件，从 `config.json` 读映射 |
| M4 | `core/conversation/checkpoint_manager.py` | 新建文件，纯文件 I/O，不含工作流编排 |
| M5 | `novel-workflow` SKILL.md | 在阶段4开头注入前序场景摘要，每场结束后写摘要 |
| M6 | `core/conversation/workflow_state_checker.py` | `generate_resume_prompt()` 加载并展示场景摘要 |

---

## M1：memory_point_sync.py 新增 writer 过滤方法

**文件**：`core/inspiration/memory_point_sync.py`

在现有 `list_recent()` 方法之后（文件末尾之前）追加以下两个方法：

```python
def search_by_writer(
    self,
    embedding: List[float],
    writer_agent: str,
    scene_type: Optional[str] = None,
    polarity: Optional[str] = None,
    top_k: int = 5,
) -> List[Dict[str, Any]]:
    """按作家角色检索记忆点。

    历史数据无 writer_agent 字段时自动回退到 search_similar()。
    """
    filter_conditions = [
        FieldCondition(key="writer_agent", match=MatchValue(value=writer_agent))
    ]
    if scene_type:
        filter_conditions.append(
            FieldCondition(key="scene_type", match=MatchValue(value=scene_type))
        )
    if polarity:
        filter_conditions.append(
            FieldCondition(key="polarity", match=MatchValue(value=polarity))
        )

    flt = Filter(must=filter_conditions)
    results = self.client.search(
        collection_name=COLLECTION_NAME,
        query_vector=embedding,
        query_filter=flt,
        limit=top_k,
    )
    hits = [{"id": r.id, "score": r.score, "payload": r.payload} for r in results]

    # 历史数据无 writer_agent 字段时回退（不过滤 writer）
    if not hits:
        hits = self.search_similar(
            embedding=embedding,
            scene_type=scene_type,
            polarity=polarity,
            top_k=top_k,
        )

    return hits


def list_recent_by_writer(
    self,
    polarity: str,
    writer_agent: str,
    top_k: int = 5,
) -> List[Dict[str, Any]]:
    """按作家角色列出最近记忆点（created_at 降序）。

    历史数据无 writer_agent 字段时回退到 list_recent()。
    """
    flt = Filter(
        must=[
            FieldCondition(key="polarity", match=MatchValue(value=polarity)),
            FieldCondition(key="writer_agent", match=MatchValue(value=writer_agent)),
        ]
    )
    results, _ = self.client.scroll(
        collection_name=COLLECTION_NAME,
        scroll_filter=flt,
        limit=top_k * 3,
        with_payload=True,
    )
    points = [{"id": str(p.id), "payload": p.payload} for p in results]
    points.sort(key=lambda x: x["payload"].get("created_at", ""), reverse=True)
    hits = points[:top_k]

    # 历史数据无 writer_agent 字段时回退
    if not hits:
        hits = self.list_recent(polarity=polarity, top_k=top_k)

    return hits
```

**测试断言**（Stage 3 直接运行）：

```python
import inspect
from core.inspiration.memory_point_sync import MemoryPointSync
src = inspect.getsource(MemoryPointSync)
assert "search_by_writer" in src
assert "list_recent_by_writer" in src
# 验证回退逻辑存在
assert "search_similar" in src.split("search_by_writer")[1].split("def ")[0]
assert "list_recent" in src.split("list_recent_by_writer")[1].split("def ")[0]
print("M1 OK")
```

---

## M2：config.json 新增 writer_mapping 节

**文件**：`config.json`

在 `"case_builder"` 节之后、最后的 `}` 之前追加：

```json
  "writer_mapping": {
    "_说明": "作家角色与专长场景类型映射，供 writer_memory_retriever.py 读取",
    "剑尘": {
      "primary_scenes": ["战斗", "高潮", "危机", "修炼突破"],
      "memory_filter": {
        "scene_types": ["战斗", "高潮", "危机"],
        "weight": 1.5
      },
      "description": "战斗设计师，专长热血战斗、功法体系、冲突张力"
    },
    "墨言": {
      "primary_scenes": ["人物", "情感", "心理", "成长"],
      "memory_filter": {
        "scene_types": ["人物", "情感", "心理"],
        "weight": 1.2
      },
      "description": "人物刻画师，专长情感细腻、心理描写、人物成长"
    },
    "云溪": {
      "primary_scenes": ["环境", "结尾", "开篇", "氛围"],
      "memory_filter": {
        "scene_types": ["环境", "氛围"],
        "weight": 1.0
      },
      "description": "意境营造师，专长氛围描写、诗意语言、美学构建"
    },
    "玄一": {
      "primary_scenes": ["悬念", "转折", "伏笔", "揭秘"],
      "memory_filter": {
        "scene_types": ["悬念", "转折"],
        "weight": 1.3
      },
      "description": "剧情编织师，专长伏笔设计、悬念布局、反转策划"
    },
    "苍澜": {
      "primary_scenes": ["开篇", "世界观", "势力", "传承"],
      "memory_filter": {
        "scene_types": ["世界观", "势力"],
        "weight": 1.0
      },
      "description": "世界观架构师，专长宏大设定、权力体系、世界规则"
    }
  }
```

**注意**：追加前确认 `case_builder` 节末尾有逗号，整个 JSON 可被 `json.loads()` 解析。

**测试断言**：

```python
import json, pathlib
cfg = json.loads(pathlib.Path("config.json").read_text(encoding="utf-8"))
wm = cfg["writer_mapping"]
assert set(wm.keys()) >= {"剑尘", "墨言", "云溪", "玄一", "苍澜"}
assert isinstance(wm["剑尘"]["memory_filter"]["weight"], float)
print("M2 OK")
```

---

## M3：新建 writer_memory_retriever.py

**文件**：`core/inspiration/writer_memory_retriever.py`（新建）

```python
# core/inspiration/writer_memory_retriever.py
"""作家专属记忆检索

从 config.json writer_mapping + MemoryPointSync 组合召回作家经验。
Embedding 由调用方传入，本模块不负责 BGE-M3 推理。
"""

from typing import Dict, Any, List, Optional

from core.inspiration.memory_point_sync import MemoryPointSync
from core.config_loader import get_project_root, load_config


def retrieve_writer_memory(
    writer_agent: str,
    current_scene_type: str,
    embedding: List[float],
    sync: Optional[MemoryPointSync] = None,
) -> Dict[str, Any]:
    """检索作家专属记忆。

    Args:
        writer_agent: 作家角色（"剑尘"/"墨言"/"云溪"/"玄一"/"苍澜"）
        current_scene_type: 当前场景类型（如 "战斗场景"）
        embedding: 场景描述向量（BGE-M3 1024维）
        sync: 可注入的 MemoryPointSync 实例（测试用）

    Returns:
        {
            "writer": str,
            "scene_type": str,
            "positive_samples": List[dict],   # 正样本
            "negative_samples": List[dict],   # 负样本
            "notes": List[str],               # 正样本中的备注
            "recommended_techniques": List[str],  # 命中的技法约束ID
            "mapping_weight": float,          # 作家权重系数
        }
    """
    if sync is None:
        sync = MemoryPointSync()

    positive = sync.search_by_writer(
        embedding=embedding,
        writer_agent=writer_agent,
        scene_type=current_scene_type,
        polarity="+",
        top_k=3,
    )

    negative = sync.search_by_writer(
        embedding=embedding,
        writer_agent=writer_agent,
        scene_type=current_scene_type,
        polarity="-",
        top_k=2,
    )

    # 从 config.json 读取作家映射配置
    cfg = load_config()
    writer_mapping = cfg.get("writer_mapping", {})
    writer_cfg = writer_mapping.get(writer_agent, {})
    mapping_weight = writer_cfg.get("memory_filter", {}).get("weight", 1.0)

    # 从正样本提取推荐技法（去重、最多取3个）
    seen = set()
    recommended_techniques: List[str] = []
    for p in positive:
        cid = p["payload"].get("used_constraint_id")
        if cid and cid not in seen:
            seen.add(cid)
            recommended_techniques.append(cid)
            if len(recommended_techniques) >= 3:
                break

    def _fmt(items: list) -> List[Dict]:
        return [
            {
                "text": i["payload"].get("segment_text", ""),
                "note": i["payload"].get("note", ""),
                "chapter": i["payload"].get("chapter_ref", ""),
            }
            for i in items
        ]

    return {
        "writer": writer_agent,
        "scene_type": current_scene_type,
        "positive_samples": _fmt(positive),
        "negative_samples": _fmt(negative),
        "notes": [
            p["payload"].get("note", "")
            for p in positive
            if p["payload"].get("note")
        ],
        "recommended_techniques": recommended_techniques,
        "mapping_weight": mapping_weight,
    }
```

**测试断言**：

```python
import inspect
from core.inspiration import writer_memory_retriever as wmr
src = inspect.getsource(wmr)
# 拼写正确
assert "recommended_techniques" in src
assert "recommended_tequences" not in src
# 不依赖独立 JSON 文件
assert "writer_scene_mapping.json" not in src
# 读 config.json
assert "load_config" in src or "writer_mapping" in src
print("M3 OK")
```

---

## M4：新建 checkpoint_manager.py

**设计说明**：本系统为**对话驱动**（Claude SKILL ↔ 用户），没有 Python 主循环。Checkpoint 的作用是：
1. **前序场景上下文注入**：阶段4开始时读取已完成场景摘要，注入给写手
2. **创作内容保存**：每场完成后由 SKILL 调用 CLI 写入快照（可选）
3. **断点恢复提示**：用户下次开始时显示"上次写到第N章第M场"

`execute_scene_with_checkpoint()` 这类"用 Python 编排场景循环"的函数**不实现**。

---

**文件**：`core/conversation/checkpoint_manager.py`（新建）

```python
# core/conversation/checkpoint_manager.py
"""场景级 Checkpoint 管理器

职责：文件 I/O。不负责工作流编排（那是 SKILL 的事）。

核心用途：
  1. 前序场景上下文注入（阶段4开始时加载已完成摘要）
  2. 每场完成后写入 200 字摘要 + 关键点
  3. 阶段7章节确认后清理当章 checkpoint

phase_sub 字段用于表示半步阶段（如 "5.5"），避免 int 字段存浮点。
"""

import json
from datetime import datetime
from pathlib import Path
from typing import Dict, Any, Optional, List
from dataclasses import dataclass, asdict, field

try:
    from core.config_loader import get_project_root
except ImportError:
    def get_project_root() -> Path:
        return Path(__file__).resolve().parents[2]


@dataclass
class SceneSummary:
    """单场景完成后的摘要记录"""
    chapter: int
    scene_index: int
    scene_type: str
    summary: str            # 200字以内摘要
    key_points: List[str]   # 3-5个关键点（角色状态、道具、伏笔等）
    writer_agent: str       # 主笔作家
    saved_at: str = ""

    def __post_init__(self):
        if not self.saved_at:
            self.saved_at = datetime.now().isoformat()


@dataclass
class WorkflowCheckpoint:
    """工作流断点记录"""
    chapter: int
    phase: int              # 主阶段 0-8
    phase_sub: Optional[str] = None   # 半步阶段，如 "5.5"（不用浮点）
    scene_index: int = 0
    scene_total: int = 0
    active_writer: Optional[str] = None
    pending_actions: List[str] = field(default_factory=list)
    checkpoint_time: str = ""
    can_resume: bool = True
    note: str = ""          # 人读的备注

    def __post_init__(self):
        if not self.checkpoint_time:
            self.checkpoint_time = datetime.now().isoformat()


class CheckpointManager:
    """Checkpoint 文件 I/O。

    存储路径：.workflow_states/{session_id}_checkpoints/
    """

    def __init__(self, session_id: str, project_root: Optional[Path] = None):
        self.session_id = session_id
        root = project_root or get_project_root()
        base = root / ".workflow_states"
        self.checkpoint_dir = base / f"{session_id}_checkpoints"
        self.checkpoint_dir.mkdir(parents=True, exist_ok=True)

    # ── 场景摘要 ──────────────────────────────────────────────

    def save_scene_summary(
        self,
        chapter: int,
        scene_index: int,
        scene_type: str,
        summary: str,
        key_points: List[str],
        writer_agent: str = "",
    ) -> str:
        """保存场景摘要（每场完成后调用）。

        Returns:
            写入的文件路径字符串
        """
        record = SceneSummary(
            chapter=chapter,
            scene_index=scene_index,
            scene_type=scene_type,
            summary=summary[:200],
            key_points=key_points[:5],
            writer_agent=writer_agent,
        )
        filename = f"ch{chapter:03d}_scene{scene_index:03d}_summary.json"
        filepath = self.checkpoint_dir / filename
        filepath.write_text(
            json.dumps(asdict(record), ensure_ascii=False, indent=2),
            encoding="utf-8",
        )
        return str(filepath)

    def load_chapter_summaries(self, chapter: int) -> List[SceneSummary]:
        """加载章节内所有已完成场景摘要（按 scene_index 排序）。

        供阶段4开始时注入上下文用。
        """
        summaries = []
        for f in sorted(self.checkpoint_dir.glob(f"ch{chapter:03d}_scene*_summary.json")):
            try:
                data = json.loads(f.read_text(encoding="utf-8"))
                summaries.append(SceneSummary(**data))
            except Exception:
                continue
        summaries.sort(key=lambda s: s.scene_index)
        return summaries

    def format_summaries_for_prompt(self, chapter: int) -> str:
        """将已完成场景摘要格式化为注入字符串。

        返回空字符串表示无历史（首场或全新章节）。
        """
        summaries = self.load_chapter_summaries(chapter)
        if not summaries:
            return ""

        lines = ["【本章已完成场景摘要】"]
        for s in summaries:
            kp = "；".join(s.key_points) if s.key_points else "无"
            lines.append(
                f"  场景{s.scene_index}（{s.scene_type}）：{s.summary}\n"
                f"  关键点：{kp}"
            )
        return "\n".join(lines)

    # ── 断点记录 ──────────────────────────────────────────────

    def save_checkpoint(
        self,
        chapter: int,
        phase: int,
        scene_index: int = 0,
        scene_total: int = 0,
        phase_sub: Optional[str] = None,
        active_writer: Optional[str] = None,
        pending_actions: Optional[List[str]] = None,
        note: str = "",
    ) -> str:
        """保存工作流断点。"""
        cp = WorkflowCheckpoint(
            chapter=chapter,
            phase=phase,
            phase_sub=phase_sub,
            scene_index=scene_index,
            scene_total=scene_total,
            active_writer=active_writer,
            pending_actions=pending_actions or [],
            note=note,
        )
        filename = f"ch{chapter:03d}_phase{phase}_checkpoint.json"
        if phase_sub:
            filename = f"ch{chapter:03d}_phase{phase}_{phase_sub}_checkpoint.json"
        filepath = self.checkpoint_dir / filename
        filepath.write_text(
            json.dumps(asdict(cp), ensure_ascii=False, indent=2),
            encoding="utf-8",
        )
        return str(filepath)

    def load_latest_checkpoint(self, chapter: int) -> Optional[WorkflowCheckpoint]:
        """加载章节最新断点。"""
        files = sorted(
            self.checkpoint_dir.glob(f"ch{chapter:03d}_*_checkpoint.json"),
            key=lambda f: f.stat().st_mtime,
            reverse=True,
        )
        if not files:
            return None
        try:
            data = json.loads(files[0].read_text(encoding="utf-8"))
            return WorkflowCheckpoint(**data)
        except Exception:
            return None

    def get_resume_description(self, chapter: int) -> str:
        """返回可读的断点恢复说明（供 generate_resume_prompt 使用）。"""
        cp = self.load_latest_checkpoint(chapter)
        if not cp:
            return ""

        phase_label = f"阶段{cp.phase}"
        if cp.phase_sub:
            phase_label = f"阶段{cp.phase}.{cp.phase_sub}"

        parts = [f"上次中断于第{cp.chapter}章 {phase_label}"]
        if cp.scene_index:
            parts.append(f"场景 {cp.scene_index}/{cp.scene_total or '?'}")
        if cp.active_writer:
            parts.append(f"当前执行：{cp.active_writer}")
        if cp.note:
            parts.append(f"备注：{cp.note}")

        return "，".join(parts)

    # ── 清理 ──────────────────────────────────────────────────

    def clear_chapter_checkpoints(self, chapter: int) -> int:
        """章节确认后清理当章所有 checkpoint 文件（阶段7调用）。

        Returns:
            删除的文件数
        """
        count = 0
        for f in self.checkpoint_dir.glob(f"ch{chapter:03d}_*.json"):
            f.unlink()
            count += 1
        return count
```

**测试断言**：

```python
import inspect, pathlib
from core.conversation.checkpoint_manager import CheckpointManager, WorkflowCheckpoint, SceneSummary

src = inspect.getsource(CheckpointManager)

# B2 修复：phase 为 int，phase_sub 为 Optional[str]
import dataclasses
fields = {f.name: f for f in dataclasses.fields(WorkflowCheckpoint)}
assert fields["phase"].type == int or str(fields["phase"].type) in ("int", "<class 'int'>")
assert "phase_sub" in fields
# 确认没有 float 比较
assert "== 5.5" not in src
assert "== 5.5" not in inspect.getsource(WorkflowCheckpoint)

# 确认没有工作流编排逻辑
assert "execute_scene" not in src
assert "phase1_outputs" not in src

# 核心方法存在
assert "save_scene_summary" in src
assert "load_chapter_summaries" in src
assert "format_summaries_for_prompt" in src
assert "clear_chapter_checkpoints" in src

# 烟雾测试：本地文件 I/O
import tempfile, pathlib
with tempfile.TemporaryDirectory() as tmp:
    mgr = CheckpointManager("test_session", project_root=pathlib.Path(tmp))
    mgr.save_scene_summary(3, 1, "战斗场景", "主角以断臂为代价击退魔族先锋", ["断臂代价", "魔族退却", "血脉初醒"], "剑尘")
    summaries = mgr.load_chapter_summaries(3)
    assert len(summaries) == 1
    assert summaries[0].scene_index == 1
    prompt = mgr.format_summaries_for_prompt(3)
    assert "断臂代价" in prompt

    mgr.save_checkpoint(3, 4, scene_index=2, scene_total=5, note="Phase2进行中")
    cp = mgr.load_latest_checkpoint(3)
    assert cp is not None
    assert cp.phase == 4
    assert cp.phase_sub is None

    mgr.save_checkpoint(3, 5, phase_sub="5", note="三方协商")
    cp2 = mgr.load_latest_checkpoint(3)
    assert cp2.phase_sub == "5"

    desc = mgr.get_resume_description(3)
    assert "阶段5" in desc

    deleted = mgr.clear_chapter_checkpoints(3)
    assert deleted == 2
    assert mgr.load_latest_checkpoint(3) is None

print("M4 OK")
```

---

## M5：novel-workflow SKILL.md 集成

**文件**：`C:\Users\39477\.agents\skills\novel-workflow\SKILL.md`

在当前文件中找到 **阶段4（逐场景创作）** 开始的位置，在执行第一个场景之前插入：

```markdown
### 前序场景上下文注入（阶段4开头执行一次）

```python
python - <<'EOF'
from core.conversation.checkpoint_manager import CheckpointManager
import sys

session_id = "CURRENT_SESSION"   # 替换为实际 session_id，或从环境变量读取
chapter = CHAPTER_NUMBER          # 替换为当前章节号

mgr = CheckpointManager(session_id)
ctx = mgr.format_summaries_for_prompt(chapter)
if ctx:
    print(ctx)
else:
    print("（本章尚无已完成场景摘要，从第1场开始）")
EOF
```

将上述输出附加到每个场景的创作提示中（"已完成场景摘要"字段）。
```

在 **每个场景完成后**（评审通过、正文确认时）插入：

```markdown
### 场景摘要写入（每场完成后执行）

```python
python - <<'EOF'
from core.conversation.checkpoint_manager import CheckpointManager

session_id = "CURRENT_SESSION"
chapter = CHAPTER_NUMBER
scene_index = SCENE_INDEX
scene_type = "SCENE_TYPE"
writer_agent = "WRITER_AGENT"

# 以下由 Claude 在确认场景时填写：
summary = "（100-200字场景摘要）"
key_points = ["关键点1", "关键点2", "关键点3"]

mgr = CheckpointManager(session_id)
path = mgr.save_scene_summary(chapter, scene_index, scene_type, summary, key_points, writer_agent)
print(f"摘要已写入：{path}")
EOF
```
```

在 **阶段7（章节最终确认）完成后** 插入：

```markdown
### 章节 Checkpoint 清理（阶段7确认后执行）

```python
python -c "
from core.conversation.checkpoint_manager import CheckpointManager
n = CheckpointManager('CURRENT_SESSION').clear_chapter_checkpoints(CHAPTER_NUMBER)
print(f'已清理 {n} 个 checkpoint 文件')
"
```
```

**注意**：上述 SKILL.md 改动为**手动编辑**，不需要 git 追踪（SKILL 文件在用户目录）。实施时直接编辑文件，编辑完成后在此处标记完成。

---

## M6：workflow_state_checker.py 增强 generate_resume_prompt

**文件**：`core/conversation/workflow_state_checker.py`

找到 `generate_resume_prompt` 方法，在其返回值构建完成前追加 checkpoint 摘要加载逻辑。

定位当前 `generate_resume_prompt` 的 `return` 语句，将其替换为：

```python
def generate_resume_prompt(self, pending: WorkflowState) -> str:
    """生成恢复提示，附加场景摘要上下文（如有）。"""
    # 原有逻辑：构建基础提示
    phase_name = ""
    if pending.workflow_type in self.WORKFLOW_TYPES:
        phase_names = self.WORKFLOW_TYPES[pending.workflow_type]["phase_names"]
        if 0 <= pending.current_phase - 1 < len(phase_names):
            phase_name = phase_names[pending.current_phase - 1]

    lines = [
        f"## 检测到未完成的工作流",
        f"",
        f"- **章节**：第 {pending.chapter} 章" if pending.chapter else "",
        f"- **当前阶段**：阶段 {pending.current_phase}（{phase_name}）",
        f"- **总阶段数**：{pending.total_phases}",
        f"- **可恢复**：{'是' if pending.can_resume else '否'}",
    ]

    if pending.scene_progress:
        lines.append(
            f"- **场景进度**：{pending.scene_progress.get('current', '?')}"
            f"/{pending.scene_progress.get('total', '?')}"
        )

    lines = [l for l in lines if l != ""]

    # 新增：加载已完成场景摘要
    if pending.chapter:
        try:
            from core.conversation.checkpoint_manager import CheckpointManager
            # session_id 从 workflow_id 取前缀（格式：{session_id}_{timestamp}）
            session_id = pending.workflow_id.rsplit("_", 1)[0] if "_" in pending.workflow_id else pending.workflow_id
            mgr = CheckpointManager(session_id, project_root=self.project_root)
            scene_ctx = mgr.format_summaries_for_prompt(pending.chapter)
            if scene_ctx:
                lines.append("")
                lines.append(scene_ctx)
            resume_desc = mgr.get_resume_description(pending.chapter)
            if resume_desc:
                lines.append("")
                lines.append(f"**断点详情**：{resume_desc}")
        except Exception:
            pass  # checkpoint 加载失败不影响主流程

    return "\n".join(lines)
```

**说明**：此处使用完整替换 `generate_resume_prompt` 方法体。实施时需先找到当前方法的起止行，整体替换。

**测试断言**：

```python
import inspect
from core.conversation.workflow_state_checker import WorkflowStateChecker
src = inspect.getsource(WorkflowStateChecker.generate_resume_prompt)
assert "CheckpointManager" in src
assert "format_summaries_for_prompt" in src
assert "get_resume_description" in src
# 确认有 try/except 保护
assert "except Exception" in src
print("M6 OK")
```

---

## 实施顺序与测试阶段

遵循 docs/opencode_dev_protocol_20260420.md：

```
阶段 0（语法检查）→ 阶段 1（单元测试）→ 阶段 2（集成测试）→ 阶段 3（断言验证）
```

**实施顺序**：M2 → M1 → M3 → M4 → M6 → M5

原因：M2（config）最先，后续代码依赖它读取 `writer_mapping`；M5（SKILL）最后，手动编辑。

### Stage 0：语法检查

```bash
python -m py_compile core/inspiration/memory_point_sync.py
python -m py_compile core/inspiration/writer_memory_retriever.py
python -m py_compile core/conversation/checkpoint_manager.py
python -m py_compile core/conversation/workflow_state_checker.py
echo "Stage 0 OK"
```

### Stage 1：单元测试（如有）

```bash
python -m pytest tests/ -x -q --tb=short 2>&1 | tee logs/test_memory_checkpoint_$(date +%Y%m%d_%H%M%S).log
```

### Stage 2：集成测试

```bash
python -c "
from core.inspiration.memory_point_sync import MemoryPointSync
from core.inspiration.writer_memory_retriever import retrieve_writer_memory
from core.conversation.checkpoint_manager import CheckpointManager
from core.conversation.workflow_state_checker import WorkflowStateChecker
print('import OK')
"
```

### Stage 3：全断言验证

```bash
python - <<'EOF'
# M1
import inspect
from core.inspiration.memory_point_sync import MemoryPointSync
src = inspect.getsource(MemoryPointSync)
assert "search_by_writer" in src
assert "list_recent_by_writer" in src
assert "search_similar" in src.split("search_by_writer")[1].split("def ")[0]
print("M1 OK")

# M2
import json, pathlib
cfg = json.loads(pathlib.Path("config.json").read_text(encoding="utf-8"))
wm = cfg["writer_mapping"]
assert set(wm.keys()) >= {"剑尘", "墨言", "云溪", "玄一", "苍澜"}
print("M2 OK")

# M3
from core.inspiration import writer_memory_retriever as wmr
src3 = inspect.getsource(wmr)
assert "recommended_techniques" in src3
assert "recommended_tequences" not in src3
assert "writer_scene_mapping.json" not in src3
print("M3 OK")

# M4
import dataclasses, tempfile
from core.conversation.checkpoint_manager import CheckpointManager, WorkflowCheckpoint, SceneSummary
fields = {f.name: f for f in dataclasses.fields(WorkflowCheckpoint)}
assert "phase_sub" in fields
src4 = inspect.getsource(CheckpointManager)
assert "== 5.5" not in src4
assert "execute_scene" not in src4
assert "save_scene_summary" in src4
assert "clear_chapter_checkpoints" in src4
with tempfile.TemporaryDirectory() as tmp:
    import pathlib
    mgr = CheckpointManager("t", project_root=pathlib.Path(tmp))
    mgr.save_scene_summary(1, 1, "战斗", "主角突破", ["突破"], "剑尘")
    assert mgr.load_chapter_summaries(1)[0].scene_index == 1
    prompt = mgr.format_summaries_for_prompt(1)
    assert "突破" in prompt
    mgr.save_checkpoint(1, 5, phase_sub="5")
    cp = mgr.load_latest_checkpoint(1)
    assert cp.phase_sub == "5"
    assert cp.phase == 5
    n = mgr.clear_chapter_checkpoints(1)
    assert n >= 1
print("M4 OK")

# M6
from core.conversation.workflow_state_checker import WorkflowStateChecker
src6 = inspect.getsource(WorkflowStateChecker.generate_resume_prompt)
assert "CheckpointManager" in src6
assert "format_summaries_for_prompt" in src6
print("M6 OK")

print("\n=== 全部断言通过 ===")
EOF
```

---

## 提交规范

```bash
git add core/inspiration/memory_point_sync.py \
        core/inspiration/writer_memory_retriever.py \
        core/conversation/checkpoint_manager.py \
        core/conversation/workflow_state_checker.py \
        config.json
git commit -m "feat: 作家记忆过滤 + 场景摘要断点续传

- memory_point_sync: 新增 search_by_writer/list_recent_by_writer，历史数据自动回退
- writer_memory_retriever: 新建，从 config.json writer_mapping 读配置
- checkpoint_manager: 新建，纯文件 I/O，save_scene_summary/format_summaries_for_prompt
- workflow_state_checker: generate_resume_prompt 注入场景摘要上下文
- config.json: 新增 writer_mapping 节（5作家场景权重配置）

修正原稿 B1(typo) B2(phase==5.5浮点) B3(删除无效Python编排)"
```
