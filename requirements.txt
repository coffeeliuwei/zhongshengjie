# 众生界小说创作支持系统依赖
# 锁定版本基于 python13 conda 环境（2026-05-01）
# torch 不锁定小版本，CUDA 后缀因机器不同而异（实测 2.11.0+cu128）

# 核心 ML 推理（向量维度/API 敏感，锁定精确版本）
sentence-transformers==5.4.1
FlagEmbedding==1.4.0
qdrant-client==1.17.1
numpy==2.4.4
torch>=2.0.0

# 系统工具
tenacity==9.1.4

# 格式转换
ebooklib==0.20
mobi==0.4.1

# 数据清洗依赖
lz4==4.4.5
jieba>=0.42.1

# 测试依赖
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0
pytest-xdist>=3.3.0
pytest-mock>=3.11.0

# 可选依赖
# transformers>=4.30.0

# Schema校验
jsonschema>=4.0.0

# Prometheus 监控指标
prometheus-client>=0.16.0

# 近重复检测
datasketch==1.10.0

# 批量提炼可选格式支持
pdfminer.six>=20221105
python-docx==1.2.0

# 去AI感管线 / 叙事台账配置（2026-05-16 新增）
PyYAML>=6.0

# 司法案例爬虫（2026-05-11 新增）
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.0
